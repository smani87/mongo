test_kind: js_test

selector:
  roots:
  - jstests/replsets/commit_transaction_rollback_recovery_data_already_applied.js

executor:
  config:
    shell_options:
      nodb: ''
      readMode: commands
